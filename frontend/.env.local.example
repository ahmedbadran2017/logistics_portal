# Copy to .env.local (gitignored) to run the dev server against a remote Frappe
# with real data. Token auth (api_key:api_secret) bypasses CSRF and never reaches
# the browser (read only by the vite proxy).
LP_BACKEND=https://admin.justyol.com
LP_API_TOKEN=your_api_key:your_api_secret
